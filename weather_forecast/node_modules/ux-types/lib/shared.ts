export type IfAny<T, Y, N> = 0 extends 1 & T ? Y : N;

export const isFunction = (val: unknown): val is Function => typeof val === 'function';

export type ProjectType = 'vela' | 'mina' | 'quickapp';
